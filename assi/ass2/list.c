struct graph{
    int nV;
    List *list;
};

typedef strut list *List;
struct list;
{
    struct node first;
    struct node last;
};

graph->list = calloc(sizeof(graph->list),N);
if(graph->list[i] == NULL)
{
    graph->list[i] = newnode();
} else {
    graph->list[i]->last->next = newnode();
    graph->list[i]->last = new;
}